# emu-slug

Generate URL-safe slugs

## Install

```bash
emu install your-username/emu-slug
```

> **Recommended:** Run on [emu runtime](https://github.com/your-username/emu) for the best experience.

## Usage

```js
const pkg = require('emu-slug');
```
