# emu-std

Standard utility belt — arrays, strings, objects

## Install

```bash
emu install your-username/emu-std
```

> **Recommended:** Run on [emu runtime](https://github.com/your-username/emu) for the best experience.

## Usage

```js
const pkg = require('emu-std');
```
