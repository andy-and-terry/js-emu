# emu-config

Layered config loader — env, file, defaults

## Install

```bash
emu install andy-and-terry/emu-config
```

> **Recommended:** Run on [emu runtime](https://github.com/andy-and-terry/emu) for the best experience.

## Usage

```js
const pkg = require('emu-config');
```
